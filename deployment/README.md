# Deployment

Deployment-oriented assets for edge and CPU environments:

- `export_onnx.py` - export sklearn pipeline to ONNX.
- `cpu_inference.py` - benchmark CPU inference with sklearn or ONNX Runtime.
- `onnx_workflow.md` - deployment workflow and edge packaging notes.
