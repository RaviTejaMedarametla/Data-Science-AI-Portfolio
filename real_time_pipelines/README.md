# Real-Time Pipelines

Unified batch + streaming inference orchestration for portfolio projects.

Run:

```bash
python real_time_pipelines/unified_pipeline.py --mode both
```

Artifacts:
- `artifacts/batch_inference.csv`
- `artifacts/stream_inference.jsonl`
